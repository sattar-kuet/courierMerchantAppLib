enum REGISTERERRORTYPE {
  Mail,
  Business,
  None,
}

enum PICKUPANDBANKDETAILS { pickup, bank, none }

enum BNAKDETAILSSTATUS { none, mobile, bank }

enum MOBILEBANKSTATUS { personal, agent }

enum CUSTOMHASDATA { None, Has, Not }

enum CUSTOMERDATAGET { None, Loading, Success }

enum PARCELPAYMENTYPE { paid, cash }
